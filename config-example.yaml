host:
  host: "127.0.0.1"
  port: 4000
  debug: true
  migrate_on_startup: false

recaptcha:
  key: "ABC"

flask:
  SECRET_KEY: "abcdef"

db:
  use_sqlite: false

  # Only relevant if use_sqlite is false.
  host: "127.0.0.1"
  port: 5432

  username: "swot"
  password: "abcdef"
  database: "swot"
